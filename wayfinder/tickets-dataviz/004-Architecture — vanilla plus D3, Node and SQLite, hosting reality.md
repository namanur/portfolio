# Architecture — vanilla + D3, Node and SQLite, hosting reality

Labels: wayfinder:research
Status: open
Assignee: (unclaimed)

## Question

D3 + vanilla JS vs framework (only compelling reasons justify React); SVG vs Canvas; data loading, state without React, responsive SVG, performance, accessibility, progressive enhancement. Node + REST + SQLite backend responsibilities (caching, key protection, ingestion, rate limits, derived metrics). SQLite on free hosts: ephemeral filesystems, serverless limits, locking, backups. Alternatives only after research: static pregenerated datasets, read-only SQLite artifact, managed Postgres, Turso/libSQL, filesystem JSON. State explicitly: GitHub Pages is static hosting and is NOT the Node backend.
